import { CalendarDays, LayoutList, Settings, ShieldCheck } from 'lucide-react'
export type AppView = 'plan' | 'history' | 'settings'
const navItems: { label: string; icon: typeof CalendarDays; view: AppView }[] = [{ label: 'План смены', icon: CalendarDays, view: 'plan' }, { label: 'История смен', icon: LayoutList, view: 'history' }, { label: 'Настройки', icon: Settings, view: 'settings' }]
type SidebarProps = { email: string; onLogout: () => void; view: AppView; onNavigate: (view: AppView) => void }
export function Sidebar({ email, onLogout, view, onNavigate }: SidebarProps) { return <aside className="sidebar"><div className="brand">Радиоплан</div><nav aria-label="Основная навигация">{navItems.map(({ label, icon: Icon, view: itemView }) => <button className={`nav-item ${view === itemView ? 'nav-item--active' : ''}`} type="button" key={label} onClick={() => onNavigate(itemView)}><Icon size={19} strokeWidth={1.8} /><span>{label}</span></button>)}</nav><div className="sidebar-footer"><button className="user-card" type="button" onClick={onLogout} title="Выйти из системы"><div className="user-avatar">ИВ</div><div><strong>{email}</strong><span>Выйти из системы</span></div></button><div className="secure-mode"><ShieldCheck size={16} /> Защищённый режим</div></div></aside> }
