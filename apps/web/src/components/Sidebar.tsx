import { CalendarDays, LayoutList, Settings } from 'lucide-react'
export type AppView = 'plan' | 'history' | 'settings'
const navItems: { label: string; icon: typeof CalendarDays; view: AppView; administratorOnly?: boolean }[] = [{ label: 'План смены', icon: CalendarDays, view: 'plan' }, { label: 'История смен', icon: LayoutList, view: 'history' }, { label: 'Настройки', icon: Settings, view: 'settings', administratorOnly: true }]
type SidebarProps = { email: string; isAdministrator: boolean; onLogout: () => void; view: AppView; onNavigate: (view: AppView) => void }
export function Sidebar({ email, isAdministrator, onLogout, view, onNavigate }: SidebarProps) { return <aside className="sidebar"><div className="brand">Радиоплан</div><nav aria-label="Основная навигация">{navItems.filter((item) => !item.administratorOnly || isAdministrator).map(({ label, icon: Icon, view: itemView }) => <button className={`nav-item ${view === itemView ? 'nav-item--active' : ''}`} type="button" title={label} aria-label={label} key={label} onClick={() => onNavigate(itemView)}><Icon size={19} strokeWidth={1.8} /><span>{label}</span></button>)}</nav><div className="sidebar-footer"><button className="user-card" type="button" onClick={onLogout} title="Выйти из системы" aria-label="Выйти из системы"><div className="user-avatar">{email.slice(0, 2).toUpperCase()}</div><div><strong>{email}</strong></div></button></div></aside> }
