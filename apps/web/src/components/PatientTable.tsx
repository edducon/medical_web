import { Check, Clock3, Pencil, Trash2 } from 'lucide-react'
import type { PatientPlan } from '../types'
import { doseFor } from './ActivityPanel'
type Props = { plans: PatientPlan[]; canConfirm: boolean; onConfirm: (id: string) => Promise<void>; onEdit?: (plan: PatientPlan) => void; onDelete?: (plan: PatientPlan) => Promise<void> }
const time = (minutes: number) => `${String(Math.floor(minutes / 60)).padStart(2, '0')}:${String(minutes % 60).padStart(2, '0')}`
export function PatientTable({ plans, canConfirm, onConfirm, onEdit, onDelete }: Props) { return <section className="panel patients-panel"><div className="table-header"><h2>Пациенты смены</h2><span>{plans.length} записей</span></div><div className="table-scroll"><table><thead><tr><th>№ пациента</th><th>Вес, кг</th><th>Протокол</th><th>Время инъекции</th><th>Расчётная активность</th><th>Статус</th><th /></tr></thead><tbody>{plans.slice().sort((a,b) => a.startMinutes-b.startMinutes).map((plan) => <tr key={plan.id}><td><strong>{plan.number}</strong></td><td>{plan.weight}</td><td><span className={`isotope-label isotope-label--${plan.isotope === 'F-18' ? 'f18' : 'ga68'}`}>{plan.isotope}</span>{plan.protocol}</td><td>{time(plan.startMinutes)}</td><td>{doseFor(plan)} МБк</td><td>{plan.confirmed ? <span className="status-button status-button--confirmed"><Check size={16}/>Подтверждено</span> : <button className="status-button" disabled={!canConfirm} onClick={() => void onConfirm(plan.id)}><Clock3 size={16}/>Ожидает врача</button>}</td><td className="row-actions">{onEdit && onDelete && <><button className="icon-button" aria-label={`Изменить ${plan.number}`} onClick={() => onEdit(plan)}><Pencil size={16}/></button><button className="icon-button" aria-label={`Удалить ${plan.number}`} onClick={() => { if (window.confirm(`Удалить запись ${plan.number}?`)) void onDelete(plan) }}><Trash2 size={16}/></button></>}</td></tr>)}</tbody></table></div></section> }
